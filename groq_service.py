from groq import AsyncGroq
from config import GROQ_API_KEY, GROQ_MODEL, GROQ_MAX_TOKENS
from typing import Dict, List, Union
import json
import re
import logging

client = AsyncGroq(api_key=GROQ_API_KEY)
logger = logging.getLogger(__name__)

class GroqService:
    
    # --- БАЗОВЫЙ МЕТОД ЗАПРОСА ---
    @staticmethod
    async def _send_groq_request(system_prompt: str, user_text: str, temperature: float = 0.5, max_tokens: int = 1000) -> str:
        try:
            response = await client.chat.completions.create(
                model=GROQ_MODEL,
                messages=[
                    {"role": "system", "content": system_prompt},
                    {"role": "user", "content": user_text}
                ],
                max_tokens=max_tokens,
                temperature=temperature
            )
            return response.choices[0].message.content.strip()
        except Exception as e:
            logger.error(f"Groq API Error: {e}")
            return ""

    # --- ЛОГИКА ---

    @staticmethod
    async def validate_ingredients(text: str) -> bool:
        prompt = """Твоя задача — модерация списка продуктов.
        Верни JSON: {"valid": true} ЕСЛИ в тексте съедобные продукты.
        Верни JSON: {"valid": false} ЕСЛИ бессмыслица, приветствия или несъедобные/опасные предметы.
        ВЕРНИ ТОЛЬКО JSON."""
        
        res = await GroqService._send_groq_request(prompt, f"Анализируй: \"{text}\"", 0.1)
        return "true" in res.lower()

    @staticmethod
    async def analyze_categories(products: str) -> List[str]:
        """
        Определяет, какие категории блюд из этого РЕАЛЬНО приготовить.
        Возвращает список ключей: ['soup', 'main', 'salad', 'dessert', 'drink', 'snack', 'sauce']
        """
        prompt = f"""Ты опытный шеф-повар. Проанализируй список продуктов: "{products}".
        
        Определи, какие категории блюд из этого РЕАЛЬНО приготовить (имея базовые соль/воду/подсолнечное масло/сахар/лёд           
           
        Возможные категории:
        - "soup" (супы)
        - "main" (вторые блюда)
        - "salad" (салаты)
        - "breakfast" (завтраки)
        - "dessert" (десерты - только если есть сахар/фрукты/мука)
        - "drink" (напитки - смузи, морсы, коктейли - если есть фрукты/молоко/ягоды)
        - "snack" (закуски)
        - "sauce" (соусы - бешамель, велуте, демиглас, томатный, голландский, винегрет, майонез, кули, фондю, ромеско)
        
        ВЕРНИ ТОЛЬКО JSON список ключей. Если продуктов очень мало, верни только самую подходящую категорию.
        Пример: ["main", "salad", "drink"]
        """
        
        res = await GroqService._send_groq_request(prompt, "Анализируй категории", 0.2)
        
        try:
            # Очистка от маркдауна, который любит Groq
            clean_json = res.replace("```json", "").replace("```", "").strip()
            data = json.loads(clean_json)
            if isinstance(data, list):
                return data
        except Exception as e:
            logger.error(f"Category JSON Error: {e}")
            pass
        
        # Fallback, если что-то пошло не так
        return ["main"]

    @staticmethod
    async def generate_dishes_list(products: str, category: str, style: str = "обычный") -> List[Dict[str, str]]:
        """
        Генерирует список блюд. Количество зависит от обилия продуктов.
        """
        # Умный подсчет ингредиентов
        items_count = len(re.split(r'[,]', products))
        
        if items_count <= 2:
            target_count = "2-3"
            complexity = "простых"
        elif items_count <= 5:
            target_count = "4-5"
            complexity = "разнообразных"
        else:
            target_count = "5-6" # Максимум 6 для экрана телефона
            complexity = "интересных"

        # Карта названий
        cat_names = {
            "soup": "Супы", "main": "Вторые блюда", "salad": "Салаты", "sauce": "Соусы",
            "breakfast": "Завтраки", "dessert": "Десерты", "drink": "Напитки", "snack": "Закуски"
        }
        cat_ru = cat_names.get(category, "Блюда")

        prompt = f"""Ты шеф-повар. Продукты: {products}.
        Задача: Придумай {target_count} {complexity} блюд в категории: "{cat_ru}". Стиль: {style}.
        
        Если категория "Напитки" - предлагай смузи, морсы, коктейли.
        
        ВЕРНИ СТРОГО JSON формат (список объектов), без лишних слов:
        [
            {{"name": "Название блюда", "desc": "Краткое описание"}}
        ]
        """
        
        res = await GroqService._send_groq_request(prompt, "Предложи меню JSON", 0.5)
        
        try:
            clean_json = res.replace("```json", "").replace("```", "").strip()
            data = json.loads(clean_json)
            if isinstance(data, list):
                return data
        except Exception as e:
            logger.error(f"Dishes JSON Error: {e}")
        return []

    @staticmethod
    async def determine_intent(user_message: str, dish_list: str) -> Dict:
        prompt = f"""Контекст: {dish_list}
        Сообщение: "{user_message}"
        Определи намерение:
        1. "add_products" (добавил продукты, изменил условия)
        2. "select_dish" (назвал блюдо текстом)
        3. "unclear"
        JSON: {{"intent": "...", "products": "...", "dish_name": "..."}}"""

        res = await GroqService._send_groq_request(prompt, "Анализируй", 0.1)
        
        try:
            start = res.find('{')
            end = res.rfind('}')
            if start != -1 and end != -1:
                return json.loads(res[start : end + 1])
        except Exception:
            pass
        return {"intent": "unclear"}
            
    @staticmethod
    async def generate_recipe(dish_name: str, products: str) -> str:
        prompt = f"""Напиши подробный рецепт: "{dish_name}".

ИСПОЛЬЗУЙ ТОЛЬКО ЭТИ ПРОДУКТЫ: {products}
МОЖНО ИСПОЛЬЗОВАТЬ БАЗОВЫЕ: соль, вода, подсолнечное масло, сахар, лед

**ВАЖНЫЕ ПРАВИЛА:**
1. Готовим СТРОГО из имеющихся продуктов (не придумывай то, чего нет в списке выше)
2. НЕ используй сразу все предоставленные продукты, а ТОЛЬКО необходимые для этого блюда
3. В списке ингредиентов указывай ТОЛЬКО используемые продукты (без лишних символов ✅/🛒)
4. Рассчитай пищевую ценность ТОЛЬКО для используемых ингредиентов
5. Используй РАЗНООБРАЗНЫЕ ЕДИНИЦЫ ИЗМЕРЕНИЯ в зависимости от продукта:
   - Для мяса/рыбы/сыра: граммы (г)
   - Для овощей/фруктов: штуки (шт.), зубчики (для чеснока)
   - Для хлеба: ломтики, кусочки
   - Для жидкостей: миллилитры (мл), столовые ложки (ст. л.), чайные ложки (ч. л.)
   - Для специй: по вкусу, щепотки
   - Для масла: для жарки, для смазывания формы

**Требования к формату:**
1. В начале укажи точное название блюда
2. Ингредиенты списком с указанием количества в ПОДХОДЯЩИХ ЕДИНИЦАХ:
   - [Ингредиент] — [количество и единица измерения];
3. Рассчитай пищевую ценность (КБЖУ) на 1 порцию в формате:
   Белки: X г
   Жиры: X г
   Углеводы: X г
   Энерг. ценность: X ккал
4. Укажи:
   - Время приготовления в минутах
   - Сложность: легкая/средняя/сложная
   - Количество порций
5. Подробное пошаговое приготовление
6. В конце добавь "Совет от шефа (Кулинарная триада):"

**Формат вывода:**
## [Название блюда]

Ингредиенты:
* [Ингредиент 1] — [количество и единица];
* [Ингредиент 2] — [количество и единица];

### Пищевая ценность на 1 порцию:
Белки: X г
Жиры: X г
Углеводы: X г
Энерг. ценность: X ккал

### Время:
X минут
Сложность: [легкая/средняя/сложная]
Порции: X человек

### Приготовление:
[Пошаговое описание процесса]

### Совет от шефа (Кулинарная триада):
[Проанализируй полученное блюдо на баланс вкусов (Жирное, Кислое, Соленое, Сладкое, Острое) и текстур (Мягкое/Хрустящее). Напиши короткий совет: чего не хватает для идеала в контексте кулинарной триады. Порекомендуй ТОЛЬКО ОДИН ингредиент! Пример: "Блюдо вышло жирным и мягким. Добавьте для баланса маринованный лук (кислота/хруст)."]"""

        res = await GroqService._send_groq_request(prompt, "Напиши рецепт с учетом всех правил", 0.4)
        if GroqService._is_refusal(res): 
            return res
        return res + "\n\n👨‍🍳 <b>Приятного аппетита!</b>"

    @staticmethod
    async def generate_freestyle_recipe(dish_name: str) -> str:
        prompt = f"""Пользователь просит рецепт: "{dish_name}".
        
        **ВАЖНЫЕ ПРАВИЛА:**
        1. Если это ЕДА -> Напиши подробный рецепт в указанном ниже формате
        2. Если это АБСТРАКЦИЯ (счастье, любовь, успех) -> Напиши метафорический рецепт в том же формате
        3. Если это ОПАСНОЕ/ЗАПРЕЩЕННОЕ (наркотики, оружие, яды, насилие) -> 
           Ответь СТРОГО: "⛔ Извините, я готовлю только еду."
        
        **Требования к формату:**
        1. В начале укажи точное название блюда/понятия
        2. Ингредиенты списком с указанием количества в ПОДХОДЯЩИХ ЕДИНИЦАХ:
           - [Ингредиент] — [количество и единица измерения];
        3. Если это реальное блюдо, рассчитай пищевую ценность (КБЖУ) на 1 порцию в формате:
           Белки: X г
           Жиры: X г
           Углеводы: X г
           Энерг. ценность: X ккал
           (Если метафорический рецепт - пропусти этот раздел)
        4. Укажи:
           - Время приготовления в минутах (или "время приготовления" для метафор)
           - Сложность: легкая/средняя/сложная
           - Количество порций
        5. Подробное пошаговое приготовление/создание
        6. В конце добавь "Совет от шефа (Кулинарная триада):" (или "Совет от философа:" для метафор)

        **Формат вывода:**
        ## [Название блюда/понятия]

        Ингредиенты:
        * [Ингредиент 1] — [количество и единица];
        * [Ингредиент 2] — [количество и единица];

        ### Пищевая ценность на 1 порцию:
        Белки: X г
        Жиры: X г
        Углеводы: X г
        Энерг. ценность: X ккал
        (Пропусти для метафорических рецептов)

        ### Время:
        X минут
        Сложность: [легкая/средняя/сложная]
        Порции: X человек

        ### Приготовление:
        [Пошаговое описание процесса]

        ### Совет от шефа (Кулинарная триада):
        [Проанализируй полученное блюдо на баланс вкусов (Жирное, Кислое, Соленое, Сладкое, Острое) и текстур (Мягкое/Хрустящее). Напиши короткий совет: чего не хватает для идеала в контексте кулинарной триады. Порекомендуй ТОЛЬКО ОДИН ингредиент!]
        
        **Если это метафора (счастье, любовь и т.д.):**
        ### Совет от философа:
        [Дай короткий философский совет по улучшению/достижению этого состояния]"""

        res = await GroqService._send_groq_request(prompt, "Напиши рецепт с учетом всех правил", 0.6)
        if GroqService._is_refusal(res): 
            return res
        return res + "\n\n👨‍🍳 <b>Приятного аппетита!</b>"

    @staticmethod
    def _is_refusal(text: str) -> bool:
        if "⛔" in text: return True
        refusals = ["cannot fulfill", "cannot answer", "against my policy", "не могу ответить", "нарушает правила"]
        for ph in refusals:
            if ph in text.lower(): return True
        return False
